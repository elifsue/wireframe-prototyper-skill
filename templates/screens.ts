import { ROUTES } from "@/routes";

// TODO: Update this array with all the project's screens after defining them in routes.ts
export const screens = [
  { path: ROUTES.HOME, label: "Home Page" },
];
